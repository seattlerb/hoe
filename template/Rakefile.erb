# -*- ruby -*-

require 'rubygems'
require 'hoe'

Hoe.spec '<%= project %>' do
  # self.rubyforge_name = '<%= project %>x' # if different than '<%= project %>'
  # p.developer('<%= XIF %>', '<%= XIF %>@example.com')
end

# vim: syntax=ruby
